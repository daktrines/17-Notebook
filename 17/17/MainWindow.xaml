<Window x:Class="_17.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_17"
        mc:Ignorable="d"
        Title="MainWindow" Height="474" Width="534.5" WindowStartupLocation="CenterScreen">
    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="423*"/>
            <RowDefinition Height="0*"/>
        </Grid.RowDefinitions>

        <Menu HorizontalAlignment="Left" Height="17" VerticalAlignment="Top" Width="526" Background="White">
            <MenuItem Header="Файл">
                <MenuItem  Header="Открыть" />
                <MenuItem  Header="Сохранить" />
                <Separator />
                <MenuItem  Header="Выход" />
            </MenuItem>
            <MenuItem  Header="Правка"/>
            <MenuItem  Header="Формат"/>
            <MenuItem  Header="Вид"/>
            <MenuItem  Header="Справка"/>
        </Menu>
        <Grid HorizontalAlignment="Stretch"  Margin="0,2,0,-4"   Grid.Row="1" Grid.RowSpan="2">
            <TextBox HorizontalAlignment="Stretch" TextWrapping="Wrap" />
        </Grid>

    </Grid>
</Window>
